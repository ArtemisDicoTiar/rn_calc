import styled, {ThemeProvider} from "styled-components/native";

const lightTheme = {
    inputColor: '#111111',
    inputBorder: '#111111',
    bgColor: '#e3e3e3',
}

const darkTheme = {
    inputColor: '#e3e3e3',
    inputBorder: '#e3e3e3',
    bgColor: '#111111',
}
